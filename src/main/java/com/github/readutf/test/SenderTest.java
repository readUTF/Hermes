package com.github.readutf.test;

import com.github.readutf.Hermes;
import redis.clients.jedis.JedisPool;
import redis.clients.jedis.JedisPoolConfig;

public class SenderTest {

    public static void main(String[] args) {
//        Hermes hermes = new Hermes(new JedisPool(new JedisPoolConfig(), "127.0.0.1", 6379, false));
//        hermes.sendParcel("test", new TestObject(), response -> System.out.println(response.getResponseType()));

    }

}
