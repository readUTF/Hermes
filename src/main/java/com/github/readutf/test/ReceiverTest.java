package com.github.readutf.test;

import com.github.readutf.Hermes;
import redis.clients.jedis.JedisPool;
import redis.clients.jedis.JedisPoolConfig;

public class ReceiverTest {

    public static void main(String[] args) {
    }

    public ReceiverTest() {
//        Hermes hermes = new Hermes(new JedisPool(new JedisPoolConfig(), "127.0.0.1", 6379, false), getClass().getClassLoader());
//        hermes.registerListener(new ListenerTest());
    }
}
